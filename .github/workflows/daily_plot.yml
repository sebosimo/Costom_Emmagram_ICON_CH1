name: Sounding Workflow

on:
  workflow_dispatch:
    inputs:
      force_refresh:
        description: 'Download fresh data (True) or use cache (False)?'
        type: boolean
        default: false
      plot_only:
        description: 'Skip fetch step entirely and only re-plot?'
        type: boolean
        default: false
  schedule:
    - cron: '0 6 * * *'

jobs:
  run-plot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      # 1. Restore Weather Data from cache
      - name: Cache Weather Data
        uses: actions/cache@v3
        with:
          path: cache_data
          key: weather-data-${{ github.run_id }}
          restore-keys: |
            weather-data-

      # 2. Setup Python with requirements.txt tracking
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      # 3. Corrected System Dependencies
      - name: Install System Dependencies
        run: |
          if ! command -v bufr_dump &> /dev/null; then
            sudo apt-get update
            sudo apt-get install -y libeccodes-dev
          else
            echo "libeccodes already installed, skipping..."
          fi

      # 4. Fast install of Python packages
      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 5. Fetch Step (Toggleable)
      - name: Fetch Data
        if: github.event.inputs.plot_only != 'true'
        env:
          FORCE_REFRESH: ${{ github.event.inputs.force_refresh }}
        run: python fetch_data.py

      # 6. Plot Step
      - name: Generate Plot
        run: python plot_skewt.py

      # 7. Final Artifact Upload
      - name: Upload Plot
        uses: actions/upload-artifact@v4
        with:
          name: skewt-plot
          path: "*.png"
